{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build:ui": "yarn install && rm -rf ./build && cd ../frontend && yarn install && yarn run build && cp -r build ../backend",
    "deploy:full": "yarn run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Jerry Tohvan",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
